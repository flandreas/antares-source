package ch.scorpion.jabbah.graph.model.net

/**
 * Unit tests for bus behaviour, multiple [OutputPort]s that are connected to the same [Net].
 */
class BusTest {
    // TODO After simulation has ported
}