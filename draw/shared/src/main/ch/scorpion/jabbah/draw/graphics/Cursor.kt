package ch.scorpion.jabbah.draw.graphics

/**
 * A class to encapsulate the target-specific bitmap representation of the mouse cursor.
 */
enum class Cursor {
    DEFAULT,
    WAIT,
    HAND,
    CROSSHAIR,
    NW_RESIZE,
    N_RESIZE,
    NE_RESIZE,
    E_RESIZE,
    SE_RESIZE,
    S_RESIZE,
    SW_RESIZE,
    W_RESIZE,
    TEXT
}